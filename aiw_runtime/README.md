# aiw_runtime — Legacy (Being Migrated)

This package currently holds some tool implementations and path validation logic.

It will be redistributed into:
- `aiw/core/`
- `aiw/providers/execution/`
- `aiw/policy/`

See `../docs/ARCHITECTURE.md` for the target structure.
