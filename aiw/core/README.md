# Core

Shared primitives, schemas, base security and utilities for the AIW platform.

**Current status:** Minimal / foundational. Most logic lives in policy, interfaces and providers for now.

Target: common types, validation, error handling that all layers depend on.
