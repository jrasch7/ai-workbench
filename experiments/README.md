# Experiments

This directory holds experimental or deprecated code that is **not part of the main architecture**.

## Current Contents

- `aiw-langgraph/` — Early LangGraph-based engineering loop spike. Not integrated with the current Agent Iterative Loop, gates, or Provider abstractions. Isolated here for reference.

See `../docs/ARCHITECTURE.md` for the approved direction.

These experiments may be:
- Re-integrated later in a controlled way (e.g. as a Planner implementation)
- Used as reference
- Eventually removed
