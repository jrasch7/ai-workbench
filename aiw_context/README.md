# aiw_context — Context Layer (To Be Reorganized)

This package provides Context Pack, indexing, and search.

It will evolve into (or feed) the Context Providers layer under `aiw/providers/context/` and `aiw/context/`.

See the full architecture in `../docs/ARCHITECTURE.md`.
